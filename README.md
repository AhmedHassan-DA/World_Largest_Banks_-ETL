# Bank Market Cap ETL Pipeline

A Python-based ETL pipeline that extracts the world’s largest banks by market capitalization, transforms values using exchange-rate data, and loads the final dataset into CSV and SQLite. Includes structured logging, multi-currency conversion, web scraping, and SQL querying.

## Project Structure

